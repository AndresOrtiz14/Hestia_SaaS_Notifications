# ── Hestia Backend ──────────────────────────────────────────────────────────────
HESTIA_API_URL=http://localhost:3000/api/v1
HESTIA_API_KEY=your-internal-api-key

# ── Worker ───────────────────────────────────────────────────────────────────────
POLLING_INTERVAL_SECONDS=10          # Frecuencia de todos los workers (segundos)
FAQ_SURVEY_DELAY_SECONDS=30          # Segundos de inactividad antes de enviar CSAT de FAQ

# ── Servidor ─────────────────────────────────────────────────────────────────────
PORT=4000

# ── WhatsApp Cloud API ──────────────────────────────────
# Per-tenant credentials (phone_id, token) come from the Property in DB.
# Only the verify_token is global (Meta webhook handshake).
WHATSAPP_VERIFY_TOKEN=your_verify_token
