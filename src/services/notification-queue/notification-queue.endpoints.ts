/** URL paths for the Notification Queue API. */
export class NotificationQueueEndpoints {
  static readonly PENDING   = 'notification-queue?status=pending';
  static readonly BY_ID     = 'notification-queue/{id}';
}
